package karmaka;

/**
 * La classe {@code Launcher} contient la méthode principale qui lance
 * l'application Karmaka.
 * Elle appelle la méthode {@code main} de la classe {@code Main}.
 *
 * <p>
 * Cette classe est utilisée pour démarrer l'application.
 * </p>
 */
public class Launcher {

    /**
     * Méthode principale qui lance l'application Karmaka en appelant la méthode
     * {@code main} de la classe {@code Main}.
     *
     * @param args Les arguments de la ligne de commande (non utilisés dans cet
     *             exemple).
     */
    public static void main(String[] args) {
        Main.main(args);
    }
}